/*
 * THIS FILE IS TOTALLY GENERATED.  DO NOT EDIT
 *
 * {{license|wordwrap(72, wrapstring='\n * ')}}
 *
 * {{description|wordwrap(72, wrapstring='\n * ')}}
 */

#ifndef {{namespace|upper}}_API_HPP_SEEN
#define {{namespace|upper}}_API_HPP_SEEN

#include "moo/apibase.hpp"

namespace {{namespace}} {

    {% for epname, ep in endpoints.items() %}

    /** {{namespace}}::{{ep.api.classname}} - API for {{epname}} protocol endpoint
     *
     * {{ep.api.description|wordwrap(72, wrapstring='\n     * ')}}
     */
    class {{ep.api.classname}} : public moo::apibase {
      public:
        {{ep.api.classname}}();

        {% for met in ep.methods %}
        /// {{met.help}}
        {{ met.type.c }} {{ met.message.name }} ( {% for f in met.message.attrs %}{{f.type.c}} {{f.name}} {{ "," if not loop.last }} {%endfor%});
        {% endfor %}
    };
    {% endfor %}

} // namespace {{namespace}}


#endif // {{namespace|upper}}_API_HPP_SEEN
