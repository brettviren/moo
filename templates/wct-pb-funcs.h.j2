// This file is 100% generated, do not edit.

#ifndef WIRECELL_PB_FUNCS
#define WIRECELL_PB_FUNCS

{% for iface in ifaces.array %}
#include "WireCellIface/I{{ iface.name }}.h"
{% endfor %}

#include "wct.pb.h"

namespace WireCell {

    // Fill a PB object from its corresponding interface.  
{%- for iface in ifaces.array %}
    void fill({{protons}}::{{protos.byname[iface.name].name}}& pbobj,
              const I{{iface.name}}::pointer& ifptr);
{%- endfor %}

    // Produce data interface pointer from corresponding PB object.
{%- for iface in ifaces.array %}
    I{{iface.name}}::pointer intern(const {{protons}}::{{protos.byname[iface.name].name}}& pbobj);
{% endfor -%}

}

#endif
